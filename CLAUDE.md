# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Status

This repository (`claude_mobile`) is newly initialized and contains no source code, build configuration, or tooling yet. The only file currently tracked is `README.md`.

Before meaningful development guidance can be documented here, the following decisions need to be made and implemented:

- **Tech stack** (e.g., React Native, Flutter, Swift/Kotlin native, Expo)
- **Package manager and build tooling**
- **Testing framework**
- **Linting and formatting**

## Next Steps for Setup

Once the stack is chosen and initialized, update this file with:

1. Build, lint, and test commands
2. How to run the app on a simulator/device
3. Project structure and architecture overview
4. Any environment variable requirements (`.env.example`)
